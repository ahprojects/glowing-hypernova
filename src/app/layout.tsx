// Root layout — required by Next.js App Router.
// The actual UI layout lives in src/app/[locale]/layout.tsx
export default function RootLayout({ children }: { children: React.ReactNode }) {
    return children;
}
